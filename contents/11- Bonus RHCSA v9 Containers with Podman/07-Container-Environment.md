# Container environment and configuration

## Exam focus

- Set environment variables, names, commands, and restart behaviour.
- Inspect the effective configuration instead of relying on the launch command alone.
- Store repeatable configuration in scripts or systemd units.

## Useful starting command

```bash
podman inspect NAME
```

> Practice on a disposable RHCSA VM or snapshot. Verify the result rather than only the command exit status.
