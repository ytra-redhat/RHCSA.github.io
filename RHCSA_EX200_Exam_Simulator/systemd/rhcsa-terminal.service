[Unit]
Description=RHCSA Exam Simulator Browser Terminal
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStartPre=-/usr/bin/tmux kill-session -t rhcsa-terminal
ExecStartPre=/usr/bin/tmux new-session -d -s rhcsa-terminal -c /tmp
ExecStart=/usr/bin/ttyd -p 7682 -W -t fontSize=16 -t fontFamily=monospace /usr/bin/tmux attach-session -t rhcsa-terminal
ExecStop=-/usr/bin/tmux kill-session -t rhcsa-terminal
Restart=on-failure
RestartSec=3
User=root
WorkingDirectory=/tmp
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
